# 📌 Настройка домена через REG.RU для Emerald ERP

## 🚀 Шаг 1: Настройка DNS записей в REG.RU

1. Войдите в личный кабинет REG.RU
2. Перейдите в раздел **"Мои домены"**
3. Найдите ваш домен и нажмите **"DNS-серверы и зона"**
4. Выберите **"Изменить зону DNS"**

### Добавьте следующие записи:

```
Тип: A
Субдомен: @ (или оставьте пустым для основного домена)
Значение: 147.45.146.149
TTL: 3600
```

```
Тип: A
Субдомен: www
Значение: 147.45.146.149
TTL: 3600
```

Если хотите поддомен (например, erp.yourdomain.ru):
```
Тип: A
Субдомен: erp
Значение: 147.45.146.149
TTL: 3600
```

## 🔧 Шаг 2: Обновление конфигурации на сервере

После настройки DNS (подождите 10-30 минут для применения), подключитесь к серверу:

```bash
ssh root@147.45.146.149
# Пароль: qWyaS2A?zg,CBa
```

### Обновите конфигурацию Nginx для вашего домена:

```bash
nano /etc/nginx/sites-available/emerald-erp
```

Замените `server_name 147.45.146.149;` на:
```nginx
server_name yourdomain.ru www.yourdomain.ru;
```

Или для поддомена:
```nginx
server_name erp.yourdomain.ru;
```

### Перезапустите Nginx:
```bash
nginx -t
systemctl reload nginx
```

## 🔐 Шаг 3: Установка SSL сертификата (HTTPS)

Установите бесплатный SSL сертификат от Let's Encrypt:

```bash
# Для основного домена:
certbot --nginx -d yourdomain.ru -d www.yourdomain.ru

# Или для поддомена:
certbot --nginx -d erp.yourdomain.ru
```

Введите ваш email и согласитесь с условиями.

## ✅ Шаг 4: Проверка

После настройки ваше приложение будет доступно по адресам:
- http://yourdomain.ru (автоматически перенаправит на HTTPS)
- https://yourdomain.ru
- https://www.yourdomain.ru

## 📝 Важные моменты:

1. **DNS обновление**: Изменения DNS могут занять от 10 минут до 24 часов
2. **SSL сертификат**: Автоматически обновляется каждые 90 дней
3. **Firewall**: Порты 80 и 443 уже открыты в настройках

## 🆘 Если что-то не работает:

### Проверьте статус приложения:
```bash
pm2 status
pm2 logs
```

### Проверьте Nginx:
```bash
systemctl status nginx
nginx -t
```

### Проверьте DNS:
```bash
nslookup yourdomain.ru
dig yourdomain.ru
```

## 📞 Контакты для поддержки REG.RU:

- Телефон: 8 (495) 580-11-11
- Онлайн-чат на сайте reg.ru
- Email: support@reg.ru

---

## 🎯 Текущий статус деплоя:

Приложение сейчас разворачивается на сервере и будет доступно через 5-10 минут по адресу:
**http://147.45.146.149**

**Данные для входа:**
- Логин: **Admin**
- Пароль: **Bereg2025**