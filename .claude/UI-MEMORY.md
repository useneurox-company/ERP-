# UI Memory - Память интерфейса

> Этот файл хранит знания об интерфейсе ERP системы.
> Claude ОБЯЗАН обновлять его после каждого тестирования!

---

## Последнее обновление
- Дата: 2024-11-30
- Что изменилось: Создан TelegramBot с диалоговым UI

---

## Telegram Bot - Концепция диалогового UI (ЭТАЛОН)

> **ОДОБРЕНО**: Этот подход утверждён как основной!

### Главные принципы:
1. **ВСЕГДА ВЫБОР** - на каждом шаге пользователь выбирает КАК взаимодействовать
2. **Кусочками** - не огромные формы, а по одному полю
3. **Три режима** (переключаемые в любой момент):
   - 💬 Свободный текст — опиши своими словами
   - 📝 По шагам — отвечай на вопросы по одному
   - 📋 Мини-форма — заполнить всё сразу

### Стандартные кнопки (на КАЖДОМ шаге):
```
[⬅️ Назад] [❌ Отмена] [💬 Помощь]
```

### Переключение режима (всегда доступно):
```
"хочу текстом" → свободный режим
"давай по шагам" → пошаговый режим
"открой форму" → мини-форма
```

### Типы ввода по данным:
| Тип данных | Как спрашиваем |
|------------|----------------|
| Дата | `📅 Когда? [Сегодня] [Завтра] [Выбрать]` |
| Телефон | `📱 Телефон: +7 ___ ___ __ __` |
| Сумма | `💰 Сумма: _______ ₽` |
| Да/Нет | `[✅ Да] [❌ Нет]` |
| Выбор 1 из N | `[Вариант 1] [Вариант 2] [Вариант 3]` |
| Количество | `[1] [2] [3] [5] [10]` или напиши число |
| Клиент | `[🔍 Найти]` или напиши имя/телефон |
| Товар | `[🔍 Найти] [📷 Фото] [💬 Опиши]` |

---

### Пример: Создание сделки

**Начало:**
```
Пользователь: "новая сделка"

Бот: Как удобнее?
     [💬 Расскажи текстом] — опиши всё своими словами
     [📝 По шагам] — отвечай на вопросы
     [📋 Форма] — заполнить всё сразу
```

**Вариант 1 - Текстом:**
```
Пользователь: "Иванов хочет 3 кресла, позвонит завтра"

Бот: Понял! Создаю:
     👤 Клиент: Иванов Пётр (нашёл в базе)
     📦 Товар: Кресло офисное ×3 = 45 000 ₽
     📅 Звонок: завтра 10:00

     [✅ Всё верно] [✏️ Исправить] [💬 Уточнить]
```

**Вариант 2 - По шагам:**
```
Бот: Клиент?
     [🔍 Найти] или напиши имя/телефон

Пользователь: "Иванов"

Бот: Нашёл:
     [1] Иванов Пётр ООО Ромашка
     [2] Иванов Сергей ИП
     [➕ Новый] [💬 Другой]

→ после выбора клиента:

Бот: Что продаём?
     [🔍 Найти товар] [📷 Фото] [💬 Опиши]

→ после выбора товара:

Бот: Количество? (напиши число или)
     [1] [2] [3] [5] [10]
```

**Вариант 3 - Форма:**
```
Бот: [Открыть форму] — заполни только пустые поля
```

---

## ScreenCreate (порт 6500)

### Страницы:
| URL | Назначение |
|-----|------------|
| `/` | Главная UI для скриншотов |
| `/crawler.html` | UI краулера |
| `/live-view.html` | Live просмотр тестов |

### Test API эндпоинты:
| Эндпоинт | Метод | Что делает |
|----------|-------|------------|
| `/test/start` | POST | Начать сессию |
| `/test/navigate` | POST | Перейти на URL |
| `/test/click` | POST | Клик по селектору |
| `/test/type` | POST | Ввод текста |
| `/test/screenshot` | POST | Скриншот |
| `/test/quick` | POST | Быстрый тест + консоль |
| `/test/console` | POST | Все логи консоли |
| `/test/console-errors` | POST | Только ошибки |
| `/test/elements` | POST | Все элементы страницы |
| `/test/end` | POST | Завершить сессию |

---

## Creatix WebStudio (порт 7000)

### Известные страницы:
| URL | Назначение | Статус |
|-----|------------|--------|
| `/` | Главная | ? |
| `/login.html` | Логин | ? |
| `/dashboard` | Дашборд | ? |

### Известные селекторы:
```javascript
// Логин
'input[name="login"]'     // Поле логина
'input[name="password"]'  // Поле пароля
'button[type="submit"]'   // Кнопка входа

// TODO: добавлять по мере обнаружения
```

---

## ERP Web Interface

### Модули и их селекторы:
<!-- Заполняется по мере тестирования -->

#### Сделки (Deals)
```javascript
// TODO: селекторы
```

#### Склад (Warehouse)
```javascript
// TODO: селекторы
```

#### Проекты (Projects)
```javascript
// TODO: селекторы
```

#### Финансы (Finance)
```javascript
// TODO: селекторы
```

---

## TelegramBot - Структура проекта

```
TelegramBot/
├── bot.js              # Главный файл бота
├── config.js           # Конфигурация
├── package.json        # Зависимости
├── .env.example        # Пример переменных окружения
├── handlers/
│   ├── commands.js     # /start, /help
│   └── messages.js     # Обработка текста и callback
└── dialogs/
    └── deal.js         # Диалог создания сделки
```

### Команды бота:
| Команда | Описание |
|---------|----------|
| /start | Приветствие и меню |
| /deal | Создать сделку |
| /cancel | Отменить действие |
| /help | Справка |

### Callback data format:
```
deal:mode:text      # Выбор режима "текстом"
deal:mode:steps     # Выбор режима "по шагам"
deal:client:select:1  # Выбор клиента с ID 1
deal:qty:5          # Выбор количества 5
deal:confirm        # Подтверждение
deal:cancel         # Отмена
```

---

## История изменений UI

### 2024-11-30 - Создан TelegramBot
- Базовая структура бота на Telegraf
- Диалог создания сделки с 3 режимами (текст/шаги/форма)
- Inline кнопки для выбора
- Session для хранения состояния

---

## Проблемные места (баги UI)

| Место | Проблема | Статус |
|-------|----------|--------|
| - | - | - |

---

## Заметки для тестирования

### Что проверять в первую очередь:
1. Консоль (consoleClean: true)
2. Основные кнопки кликабельны
3. Формы отправляются
4. Данные не undefined/null

### Частые ошибки:
- [ ] TODO: добавлять по мере обнаружения
